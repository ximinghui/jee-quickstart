# 该文件为EditorConfig的配置文件，不具有项目侵入性，主要用于控制项目文件的文本风格。
# 当该文件存在且有可用EditorConfig插件时会生效，若不需要该特征则直接移除该文件即可。
# 许多主流IDE和文本编辑器已经内置了EditorConfig的支持，如Visual Studio Code、
# Visual Studio、IntelliJ IDEA等，具体查阅官网（https://editorconfig.org/）
# 来确认你的IDE是否需要安装插件。以下介绍取自EditorConfig官网：

# What is EditorConfig?
# EditorConfig helps maintain consistent coding styles for multiple
# developers working on the same project across various editors and
# IDEs. The EditorConfig project consists of a file format for defining
# coding styles and a collection of text editor plugins that enable
# editors to read the file format and adhere to defined styles.
# EditorConfig files are easily readable and they work nicely with
# version control systems.

# EditorConfig根配置文件
root = true

# 该EditorConfig配置文件本身
[.editorconfig]
end_of_line = crlf
insert_final_newline = true
charset = utf-8

# Java源码文件
[*.java]
end_of_line = crlf
insert_final_newline = true
charset = utf-8
indent_style = space
indent_size = 4
